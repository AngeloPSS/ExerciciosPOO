using ExerciciosPOO.Questao1;
/*internal class Program {
    private static void Main(string[] args) {
        Animal[] VetorAnimais = new Animal[20];
        string x;
        for (int i = 0; i < 20; i++) {
            if (i % 2 == 0 && i % 3 != 0) {
                x = "gato";
            } else if (i % 6 == 0) {
                x = "cachorro";
            } else {
                x = "paparagaio";
            }
            Animal animal = new Animal(i.ToString(), x);
            VetorAnimais[i] = animal;
        }

        foreach (Resultado animal in Animal.ContaEspecie(VetorAnimais)){
            Console.WriteLine(animal.nomeEspecie + " " + animal.quantidade);
        }
        ;
    }
}*/
